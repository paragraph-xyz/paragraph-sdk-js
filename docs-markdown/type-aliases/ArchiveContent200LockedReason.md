[**@paragraph-com/sdk**](../README.md)

***

[@paragraph-com/sdk](../README.md) / ArchiveContent200LockedReason

# Type Alias: ArchiveContent200LockedReason

> **ArchiveContent200LockedReason** = `string` \| `null`

Defined in: src/generated/models/archiveContent200LockedReason.ts:18

Why this piece can't be edited right now, or null when it can. A queued or in-flight send locks the words, because they go out exactly as written.
