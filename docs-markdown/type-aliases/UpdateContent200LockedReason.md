[**@paragraph-com/sdk**](../README.md)

***

[@paragraph-com/sdk](../README.md) / UpdateContent200LockedReason

# Type Alias: UpdateContent200LockedReason

> **UpdateContent200LockedReason** = `string` \| `null`

Defined in: src/generated/models/updateContent200LockedReason.ts:18

Why this piece can't be edited right now, or null when it can. A queued or in-flight send locks the words, because they go out exactly as written.
