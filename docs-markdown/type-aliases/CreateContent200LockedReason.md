[**@paragraph-com/sdk**](../README.md)

***

[@paragraph-com/sdk](../README.md) / CreateContent200LockedReason

# Type Alias: CreateContent200LockedReason

> **CreateContent200LockedReason** = `string` \| `null`

Defined in: src/generated/models/createContent200LockedReason.ts:18

Why this piece can't be edited right now, or null when it can. A queued or in-flight send locks the words, because they go out exactly as written.
