{
  "name": "@paragraph_xyz/sdk",
  "version": "0.0.10",
  "description": "TypeScript SDK for Paragraph API",
  "repository": {
    "url": "https://github.com/paragraph-xyz/paragraph-sdk-js"
  },
  "main": "./dist/index.js",
  "module": "./dist/index.mjs",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup",
    "generate": "orval --config orval.config.ts && typedoc",
    "prepublishOnly": "yarn generate && yarn build",
    "release:patch": "npm version patch && git push && git push --tags",
    "release:minor": "npm version minor && git push && git push --tags",
    "release:major": "npm version major && git push && git push --tags"
  },
  "dependencies": {
    "axios": "^1.7.7"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "orval": "^7.11.2",
    "tsup": "^8.3.5",
    "typedoc": "^0.28.12",
    "typedoc-plugin-markdown": "^4.8.1",
    "typescript": "^5.0.0"
  },
  "publishConfig": {
    "access": "public"
  },
  "packageManager": "yarn@3.4.1"
}
